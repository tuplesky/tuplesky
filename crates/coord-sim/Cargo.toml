[package]
name = "coord-sim"
description = "Deterministic world, controlled I/O, workloads and independent checkers (test-only)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[package.metadata.tuplesky]
# Never linked into production artifacts; enforced by `cargo xtask check-deps`.
role = "test-only"

[dependencies]
coord-types.workspace = true
coord-core.workspace = true
serde = { workspace = true, features = ["std"] }
serde_json.workspace = true
rand_chacha.workspace = true
rand_core.workspace = true
blake3.workspace = true

[dev-dependencies]
tempfile.workspace = true
